



















  for(int j = i+1; j< toSort.length; j++) {
      System.out.println("In inner loop and the counter is " + j);
      if (toSort[j]<toSort[highestIndex]) {
          highestIndex=j;
          System.out.println("In if statement and the highest index is " + highestIndex);
      }
  }
  temp = toSort[highestIndex];
  toSort[highestIndex]=toSort[i];
  toSort[i]=temp;
}
for (int i = 0;i < toSort.length;i++)  {
    System.out.println(toSort[i]);
}
